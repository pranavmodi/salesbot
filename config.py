# SMTP Server Configuration Information (Host and Port loaded from .env)

# Non-sensitive Email Settings
EMAIL_SUBJECT = "Following up" # You might want to personalize this further later

# Sensitive credentials (SENDER_EMAIL, SENDER_PASSWORD) are now loaded from .env
# EMAIL_DELAY_MINUTES is now configured in .env file 